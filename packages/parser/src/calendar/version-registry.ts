import type { VersionDefinition } from '../types/calendar.js';

export function parseVersionRegistry(_content: string): VersionDefinition[] {
  throw new Error('Version registry parser is not implemented in Phase 1 scaffold.');
}
