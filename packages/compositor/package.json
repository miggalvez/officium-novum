{
  "name": "@officium-novum/compositor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "adjudications:fanout": "node ./test/divergence/fanout-adjudications.mjs --write",
    "build": "tsc -p tsconfig.json",
    "compare:phase-3-perl": "pnpm -C ../parser build && pnpm -C ../rubrical-engine build && pnpm build && node ./test/fixtures/compare-phase-3-perl.mjs",
    "compare:phase-3-perl:full": "pnpm compare:phase-3-perl -- --date __full-year__",
    "report:phase-3-progress": "node ./scripts/report-phase-3-progress.mjs",
    "test": "vitest run",
    "test:no-throw": "node ./scripts/no-throw-sweep.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:phase-3-signoff": "node ./scripts/verify-phase-3-signoff.mjs"
  },
  "dependencies": {
    "@officium-novum/parser": "workspace:*",
    "@officium-novum/rubrical-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
